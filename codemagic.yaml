workflows:
  android-app:
    name: Android App
    environment:
      java: 11
    scripts:
      - name: Decode keystore
        script: echo $KEYSTORE | base64 --decode > my_keystore.jks
      - name: Build APK
        script: ./gradlew assembleRelease
    artifacts:
      - app/build/outputs/apk/release/*.apk
